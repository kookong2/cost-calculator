# 대행 계산기 작업 규칙

## 기본 원칙

- 사용자와의 대화, 설명, 완료 보고는 한국어로 작성한다.
- 결과를 먼저 말하고 설명은 간결하게 작성한다.
- 작업 전 저장소 구조, Git 상태, 기존 문서와 규칙을 먼저 확인한다.
- 사용자가 만든 기존 변경사항을 보존하고 요청 범위 밖의 수정은 하지 않는다.
- 새 운영 의존성이나 빌드 도구를 추가하기 전에 필요성과 대안을 설명한다.
- 비밀키, 토큰, 비밀번호, `.env` 파일을 커밋하지 않는다.
- 변경 후 위험도에 맞는 테스트와 브라우저 검증을 실행한다.
- 완료 시 변경 내용, 검증 결과, 남은 문제를 짧게 요약한다.

## 프로젝트 특성

- 이 프로젝트는 `index.html`, `sw.js`, `manifest.webmanifest`로 구성된 정적 PWA다.
- 현재 별도 프레임워크, 패키지 관리자, 번들러, 테스트 러너를 사용하지 않는다.
- 단순 변경은 기존 단일 파일 구조와 순수 HTML/CSS/JavaScript 방식을 우선한다.
- 구조 분리나 도구 도입은 복잡도가 실제로 낮아지는 경우에만 제안하고 사용자 승인 후 진행한다.
- 사용자 화면의 기본 언어와 금액 표시는 기존 한국어 형식을 유지한다.

## 계산 로직 안전 규칙

- 기존 환율, 수수료, 부가율, 고정비, 반올림 규칙을 임의로 변경하지 않는다.
- 계산식을 변경할 때는 변경 전후 공식과 예시 입력·예상 결과를 먼저 명시한다.
- 빈 값, 0, 음수, 소수, 매우 큰 값과 숫자가 아닌 입력을 고려한다.
- 화면 표시용 문자열과 실제 계산용 숫자를 분리해 처리한다.
- 복사 기능에는 사용자에게 보이는 최종 결과만 포함되도록 확인한다.

## PWA 및 배포 안전 규칙

- 정적 자산이나 `index.html`을 변경하면 `sw.js`의 캐시 전략과 캐시 버전 갱신 필요성을 확인한다.
- 이전 서비스워커 캐시 때문에 구버전 화면이 노출되지 않는지 검증한다.
- 로컬 검증 없이 GitHub Pages에 반영하지 않는다.
- 커밋, 푸시, GitHub Pages 배포는 각각 사용자의 명시적 승인을 받은 뒤 진행한다.
- 배포 요청을 받으면 로컬 검증 결과와 배포 대상 변경사항을 먼저 요약한다.

## 작업별 지침

- 개발 작업은 `docs/codex/development.md`를 추가로 읽고 따른다.

